# 地基雷达监测报告生成器 Mobile V2 完整设置教程

## 一、这次更新包含什么

Mobile V2 保留 6 小时和 24 小时报告，并增加“一周报告”。一周报告支持：

- 自动推荐本周 7 天时间段；
- 自动推荐紧邻的上周 7 天对比时间段；
- 生成一周文字报告；
- 复制、分享、保存 TXT；
- 导出“一周对比表.xlsx”；
- 24 小时 Excel 对比表继续保留。

## 二、如果已经部署过 Mobile V1：推荐直接更新原 GitHub 仓库

不需要重新新建仓库，也不需要重新设置 Pages。

### 步骤 1：解压 V2 ZIP

在电脑上解压 `radar_report_mobile_week_v2.zip`。

打开文件夹后应直接看到：

- index.html
- app.js
- styles.css
- manifest.webmanifest
- sw.js
- icon-192.png
- icon-512.png
- README.txt
- 完整设置教程.md

### 步骤 2：进入原 GitHub 仓库

进入之前用于部署手机版的仓库，打开 `Code` 页面。

### 步骤 3：上传并替换 V2 文件

选择 `Add file` → `Upload files`。

把 V2 文件夹中的全部文件拖到上传区域并提交到 `main` 分支。核心要求是：`index.html`、`app.js`、`styles.css`、`sw.js` 必须位于发布源的根目录。

如果 GitHub 网页上传时对同名文件处理不方便，可以先删除旧的同名文件，再上传 V2；不要把 V2 整个文件夹再套一层目录。

正确层级：

```
radar-report-mobile/
├─ index.html
├─ app.js
├─ styles.css
├─ manifest.webmanifest
├─ sw.js
├─ icon-192.png
├─ icon-512.png
├─ README.txt
└─ 完整设置教程.md
```

错误层级：

```
radar-report-mobile/
└─ radar_report_mobile_week_v2/
   ├─ index.html
   └─ ...
```

### 步骤 4：等待 GitHub Pages 自动重新部署

如果原仓库已经设置过：

- Settings → Pages
- Source = Deploy from a branch
- Branch = main
- Folder = /(root)

那么通常不需要再改设置。提交 V2 后 Pages 会重新发布。

可以进入仓库的 `Actions` 页面，看 Pages 构建任务是否变成绿色勾。

### 步骤 5：电脑浏览器检查 V2

重新打开原 Pages 网址。

页面顶部应看到：

`Mobile V2 · 6h / 24h / 周报 · 本机处理`

并且报告页签应变成 3 个：

- 6 小时
- 24 小时
- 一周

如果仍然显示 Mobile V1：

1. 按 Ctrl+F5 强制刷新电脑浏览器；
2. 等待 GitHub Pages 构建完成；
3. 再刷新一次。

## 三、如果从未设置 GitHub Pages：完整首次部署

### 1. 新建 GitHub 仓库

登录 GitHub → 右上角 `+` → `New repository`。

建议仓库名：`radar-report-mobile`。

使用 GitHub Free 时，推荐选择 Public。

### 2. 上传文件

进入仓库 → `Add file` → `Upload files` → 上传 V2 文件夹里的所有文件 → Commit changes。

### 3. 开启 Pages

进入：`Settings` → 左侧 `Pages`。

设置：

- Source：`Deploy from a branch`
- Branch：`main`
- Folder：`/(root)`

然后点击 `Save`。

### 4. 等待发布

进入 `Actions` 查看 `pages build and deployment`。绿色勾表示完成。

Pages 地址一般为：

`https://GitHub用户名.github.io/仓库名/`

## 四、iPhone 安装成主屏幕 App

### 1. 用 Safari 打开 GitHub Pages 地址

建议把网址从电脑微信发到“文件传输助手”，在 iPhone 点击后选择“在 Safari 中打开”；也可以复制网址后直接粘贴到 Safari。

### 2. 添加到主屏幕

在 Safari 打开网页后：

1. 点击 Safari 的分享按钮；
2. 找到“添加到主屏幕”；
3. 如果系统显示“作为 Web App 打开 / Open as Web App”，将其打开；
4. 名称建议保持“雷达报告”；
5. 点击“添加”。

之后 iPhone 桌面会出现“雷达报告”图标。

### 3. 从 V1 更新到 V2 后的特别操作

本版本已经把缓存版本升级到 V2，并会清理旧 V1 缓存。更新后建议：

1. 先用 Safari 打开 Pages 网址；
2. 下拉/刷新，确认顶部显示 `Mobile V2`；
3. 再打开桌面的“雷达报告”。

如果桌面图标仍长期显示旧 V1，可删除旧的主屏幕 Web App，再按“添加到主屏幕”重新添加一次。

## 五、iPhone 日常使用

### 1. 下载监测数据

从手机浏览器把两份雷达 Excel 下载到 iPhone“文件”App，一般位于“下载项”。

### 2. 打开“雷达报告”

点击主屏幕图标。

### 3. 选择两个 Excel

- 面数据：M1、M2、M3……
- 点数据：S1、S2、S3……

然后点“加载数据”。

程序会取两份文件的共同时间点。

### 4. 生成 6 小时报告

进入“6 小时” → “自动推荐” → “生成报告”。

生成后可：复制报告、分享、保存 TXT。

### 5. 生成 24 小时报告

进入“24 小时” → “自动推荐” → “生成报告”。

生成后可：复制报告、分享、保存 TXT、导出 Excel 对比表。

### 6. 生成一周报告

进入“一周” → “自动推荐”。

程序会：

- 以最新共同数据时刻作为本周期末；
- 向前约 7 天寻找最接近的共同时间点作为本周开始；
- 再向前约 7 天寻找上周开始；
- 本周开始时刻同时作为上周结束时刻。

然后点击“生成报告”。

生成后可：

- 复制报告；
- 系统分享；
- 保存“一周监测报告.txt”；
- 导出“一周对比表.xlsx”。

如果数据历史不足约 14 天，一周报告仍然可以生成，但文字中会提示无法计算与上周相比的位移变化，Excel 也不会添加上周和周环比列。

## 六、一周 Excel 对比表格式

包含两个工作表：

- 面
- 点

有上周数据时，列为：

1. 监测点/面
2. 本周（7天）时段位移(mm)
3. 上周（7天）时段位移(mm)
4. 与上周相比变化量(mm)
5. 截至期末总累积位移(mm)

“与上周相比变化量” = 本周位移 − 上周位移。

## 七、常见问题

### 1. 页面还是 V1，没有“一周”页签

先确认 GitHub `Actions` 中 Pages 已部署成功，再强制刷新。iPhone 上先用 Safari 打开网址并刷新；必要时删除旧主屏幕图标后重新添加。

### 2. 打开网页提示 Excel 组件未加载

首次使用保持联网，然后刷新网页。SheetJS Excel 组件成功加载后再选择文件。

### 3. “加载失败：没有共同时间点”

检查是否把面数据和点数据选反；同时确认两份 Excel 是否来自相同监测时段。

### 4. 一周对比周期为空

通常表示数据历史不足 14 天左右，或者目标 7 天时刻附近缺少共同数据。可以手动选择有效时间，也可以只生成本周报告。

### 5. GitHub Pages 404

确认 `index.html` 位于发布源根目录，并确认 Settings → Pages 的分支和目录设置正确。

## 八、数据隐私

当前 Mobile V2 没有把用户选择的 Excel 上传到服务器的代码。Excel 由 iPhone/浏览器本地读取和计算。GitHub Pages 只托管网页程序文件。
