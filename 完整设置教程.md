# 地基雷达监测报告生成器 Mobile V4 — 完整部署与 iPhone 使用教程

## 1. V4 与 V3 的关系

V4 直接替换 V3，不需要重新注册 GitHub、不需要新建仓库，也不需要更换 GitHub Pages 网址。

若原仓库为：

`radar-report-mobile`

且原网址类似：

`https://<GitHub用户名>.github.io/radar-report-mobile/`

升级后仍使用同一仓库和同一网址。

## 2. V4 最重要的修正

- 24h 日报正文不再出现“与上周相比”。
- 24h 页面仍保留“Excel 对比周期”，但仅用于导出 24h 对比表。
- 周报仍保留“与上周相比”。
- Excel 总累积位移直接取期末累计值。
- 6h 自动推荐按真实 6 小时段查找。
- 增加 M1-M14 / S1-S14 必要字段检查。

## 3. 解压完整包

将 `radar_report_mobile_v4.zip` 解压。

进入 `radar_report_mobile_v4` 文件夹后，第一层应看到：

- `index.html`
- `app.js`
- `styles.css`
- `manifest.webmanifest`
- `sw.js`
- `.nojekyll`
- `icon-192.png`
- `icon-512.png`
- `wjz_shibao_zhong_V4_fixed.py`
- `README.txt`
- `V4修正说明.txt`
- `完整设置教程.md`

上传 GitHub 时，应上传这些文件本身，不要把外层 `radar_report_mobile_v4` 文件夹再套进仓库。

## 4. 在 GitHub 中用 V4 覆盖 V3

1. 电脑浏览器登录 GitHub。
2. 打开原来的 `radar-report-mobile` 仓库。
3. 点击 `Code`。
4. 选择 `Add file` → `Upload files`。
5. 把 V4 解压目录中的文件上传到仓库根目录。
6. 对 `index.html`、`app.js`、`styles.css`、`manifest.webmanifest`、`sw.js` 等同名文件执行覆盖更新。
7. Commit message 可填写：`Update radar report mobile to V4`。
8. 点击 `Commit changes`。

仓库根目录应直接看到 `index.html`，不要出现：

`radar-report-mobile/radar_report_mobile_v4/index.html`

## 5. GitHub Pages 设置

如果 V3 已经发布成功，一般无需修改。

进入：`Settings` → `Pages`

确认：

- Source：`Deploy from a branch`
- Branch：`main`
- Folder：`/(root)`

## 6. 等待部署

进入仓库顶部 `Actions`。

找到 `pages build and deployment`：

- 黄色：正在部署
- 绿色勾：部署成功
- 红色叉：部署失败

必须等到绿色勾后再测试。

## 7. 电脑端确认已经是 V4

打开原 GitHub Pages 地址。

建议按 `Ctrl + F5` 强制刷新。

页面顶部应看到：

`V4 · 双数据源 · 6小时 / 24小时 / 周报`

24h 页面应看到：

`Excel 对比周期（仅用于对比表，不写入 24h 日报正文）`

这两处都出现即表示 V4 文件已经生效。

## 8. iPhone 从 V3 升级到 V4

第一次升级不要只点旧主屏幕图标。

1. 在 iPhone Safari 中直接打开原 GitHub Pages 地址。
2. 刷新页面。
3. 确认页面顶部显示 V4。
4. 进入 24h 页，确认提示“对比周期仅用于 Excel”。

V4 的 Service Worker 使用新的缓存名 `radar-report-v4`，会清理旧 V3 缓存。

如果 Safari 已显示 V4，但主屏幕旧 Web App 仍显示 V3：

1. 长按旧“雷达报告”图标。
2. 删除旧 Web App。
3. Safari 再次打开 V4 页面。
4. 点击“分享”。
5. 选择“添加到主屏幕”。
6. 名称可设为“雷达报告V4”。
7. 添加。

## 9. V4 日常加载数据

V4 仍使用四个 Excel：

- 短时面（小时级 M 数据）
- 短时点（小时级 S 数据）
- 长时面（日级 M 数据）
- 长时点（日级 S 数据）

全部选择后点击“加载全部数据”。

6h 使用短时数据计算时段位移，长时数据提供截止期末总累积位移。

24h 与周报使用长时数据。

## 10. 24h 报告的正确用法

点击 `24小时` → `自动推荐` → `生成报告`。

正文只输出当前 24 小时三个分区的位移范围、最大值位置、期末总累积位移。

正文不会出现：

`与上周相比……`

页面中的“Excel 对比周期”仍会自动选择前一个 24h 周期，它只用于点击“导出 Excel”后生成对比列，不参与日报文字。

## 11. 周报的正确用法

点击 `周报` → `自动推荐（周五节点）` → `生成报告`。

周报仍会根据当前 7 天与前一周 7 天计算：

`与上周相比，监测面位移变化……，监测点位移变化……。`

这是周报专属逻辑。

## 12. Excel 修正说明

V4 导出的 24h/周报 Excel 中：

`总累积位移量(mm)`

直接取源数据在期末时刻（或最接近期末的长时节点）的累计位移值。

V3 中错误使用了“期末累计值 - 数据首期累计值”，可能导致导出 Excel 与报告正文不一致；V4 已统一。

## 13. 出现旧版缓存时

按顺序处理：

1. 确认 GitHub Actions 已绿。
2. 电脑 Ctrl+F5 看是否 V4。
3. iPhone Safari 打开网址并刷新。
4. 仍显示 V3 时，关闭 Safari 页面重新打开。
5. 主屏幕 App 仍旧时，删除旧 Web App 并重新添加。

## 14. 数据隐私

Excel 文件由 iPhone Safari/网页 App 在本机浏览器中读取和计算。程序本身部署在 GitHub Pages；选择的监测 Excel 不会因为使用该页面而自动提交到 GitHub 仓库。

当前 Excel 解析库由 SheetJS CDN 加载，因此第一次打开或缓存尚未建立时建议保持联网。
