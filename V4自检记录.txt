Mobile V4 自检记录
=================

1. Python 语法检查：通过（python -m py_compile）。
2. 24h 正文逻辑测试：通过；生成文本中不存在“与上周相比”。
3. 周报比较逻辑测试：通过；有有效前一周周期时仍生成“与上周相比”。
4. Excel 总累积位移测试：通过；测试数据期末累计值为 30.0 时，导出表总累积位移为 30.0，而不是旧逻辑的 20.0（30-10）。
5. JavaScript 语法检查：通过（node --check）。
6. Service Worker 缓存名：radar-report-v4，可与 V3 缓存区分并清理旧缓存。
